/**
 * Text chunking.
 * Implemented in Phase B (ingest pipeline):
 * splits extracted PDF text into ~1000-char chunks with 200 overlap
 * using LangChain's RecursiveCharacterTextSplitter.
 */

// TODO (Phase B): implement chunkText(text) with RecursiveCharacterTextSplitter
