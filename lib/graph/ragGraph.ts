/**
 * LangGraph RAG pipeline.
 * Implemented in Phase C (query pipeline):
 * retrieve -> grade -> generate / no-data, as described in the plan.
 */

// TODO (Phase C): build the LangGraph state graph for the chat flow
