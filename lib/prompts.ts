/**
 * RAG prompts.
 * Implemented in Phase C (query pipeline):
 * system + user prompts that force the LLM to answer only from
 * the retrieved context, or say "sorry, no data found".
 */

// TODO (Phase C): define RAG system prompt and answer prompt template
