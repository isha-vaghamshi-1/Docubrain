/**
 * OpenAI embeddings client.
 * Implemented in Phase B (ingest) and reused in Phase C (query):
 * wraps OpenAIEmbeddings (text-embedding-3-small, dim 1536).
 */

// TODO (Phase B): export a shared OpenAIEmbeddings instance
